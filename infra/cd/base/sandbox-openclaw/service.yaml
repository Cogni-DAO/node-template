apiVersion: v1
kind: Service
metadata:
  name: sandbox-openclaw
spec:
  selector:
    app.kubernetes.io/name: sandbox-openclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
      protocol: TCP
