# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# Single-node (node-template) applicationset for Cogni-DAO/node-template.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cogni-candidate-a
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/cogni-dao/node-template.git
        revision: deploy/candidate-a-node-template
        files:
          - path: "infra/catalog/node-template.yaml"
  template:
    metadata:
      name: "candidate-a-{{.name}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/cogni-dao/node-template.git
        targetRevision: "deploy/candidate-a-{{.name}}"
        path: "infra/k8s/overlays/candidate-a/{{.name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cogni-candidate-a
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
