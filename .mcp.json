{
  "mcpServers": {},
  "_disabledMcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    },
    "grafana": {
      "command": "bash",
      "args": [
        "-lc",
        "set -euo pipefail; set -a; test -f .env.local && source .env.local; set +a; docker rm -f mcp-grafana >/dev/null 2>&1 || true; exec docker run --name mcp-grafana --rm --init -i -e GRAFANA_URL -e GRAFANA_SERVICE_ACCOUNT_TOKEN mcp/grafana -t stdio"
      ]
    },
    "grafana-local": {
      "command": "bash",
      "args": [
        "-lc",
        "docker rm -f mcp-grafana-local >/dev/null 2>&1 || true; exec docker run --name mcp-grafana-local --rm --init -i --network web -e GRAFANA_URL=http://grafana:3000 mcp/grafana -t stdio"
      ]
    },
    "assistant-ui": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@assistant-ui/mcp-docs-server"],
      "env": {}
    }
  }
}
