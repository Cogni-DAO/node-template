---
id: x402-e2e-spec
type: spec
title: "x402 End-to-End: Hyperbolic Provider + Per-Request Settlement"
status: draft
spec_state: draft
trust: draft
summary: Replace OpenRouter credit top-ups with x402 upto scheme end-to-end — users pay Cogni nodes per-request, nodes pay Hyperbolic per-request, all in USDC on Base. No credit system, no Privy, no Splits, no Coinbase Commerce.
read_when: Working on x402 integration, Hyperbolic provider migration, or payment architecture changes.
owner: derekg1729
created: 2026-02-26
verified:
tags: [billing, x402, web3, provider]
---

# x402 End-to-End: Hyperbolic Provider + Per-Request Settlement

> [!CRITICAL]
> This spec replaces the entire credit-purchase-then-consume billing model with x402 per-request settlement. The DB credit system (credit_ledger, payment_attempts, billing_accounts balance tracking) is deleted. LiteLLM remains the cost oracle. charge_receipts remain the audit trail.

## Context

The current billing architecture requires users to pre-purchase credits (USDC → on-chain verify → mint credits in DB), then consume credits per-request (LiteLLM → charge_receipts → debit credit_ledger). The outbound leg requires an operator wallet (Privy), Splits contract, and Coinbase Commerce integration to top up OpenRouter.

x402's `upto` scheme (shipped by Thirdweb, SDK v5.114.0+) enables a fundamentally simpler model: per-request dynamic settlement where the client authorizes a maximum amount and the server settles the actual cost after completion. Combined with Hyperbolic (x402 launch partner, open-source GPU inference), this enables **symmetric x402 on both legs** — no credit system, no pre-funding, no fiat bridge.

**Tradeoff:** Hyperbolic serves open-source models only (DeepSeek, Llama, Qwen, Mixtral). No Claude, GPT, or Gemini. This spec accepts that tradeoff for architectural simplicity and full crypto-native sovereignty.

## Goal

Symmetric x402 payment architecture: users/agents pay Cogni nodes per-request via x402 `upto`, nodes pay Hyperbolic per-request via x402 `upto`. LiteLLM routes requests and computes cost. No credit balances, no deposit flow, no operator wallet, no fiat bridge.

## Non-Goals

| Item | Reason |
| - | - |
| Proprietary model access (Claude, GPT, Gemini) | Hyperbolic is open-source only. Hybrid OpenRouter fallback is a separate spec if needed. |
| COGNI utility token | x402 USDC per-request eliminates the need for a token on both legs. |
| Operator wallet (Privy) | No pre-funded provider balance needed — x402 settles per-request. |
| Splits contract | No revenue routing needed — DAO margin is computed and settled atomically per-request. |
| Coinbase Commerce integration | No fiat bridge — Hyperbolic accepts x402 USDC directly. |
| L402 (Lightning) support | P2+ consideration. This spec is x402-only. |
| Credit holds / pre-call reservations | x402 `upto` authorization IS the reservation. |

## Core Invariants

1. **X402_SYMMETRIC**: Both inbound (user→node) and outbound (node→Hyperbolic) use x402 `upto` scheme on Base with USDC. No asymmetry.

2. **LITELLM_COST_ORACLE**: LiteLLM computes per-request cost via `x-litellm-response-cost` header (non-streaming) or `usage.cost` in final usage event (streaming). This is unchanged from current billing.

3. **CHARGE_RECEIPTS_IMMUTABLE**: charge_receipts remain the immutable audit trail. Each receipt now includes `x402_settlement_tx` for on-chain proof. Idempotency via `UNIQUE(source_system, source_reference)` is unchanged.

4. **DAO_MARGIN_ATOMIC**: DAO margin is computed per-request: `user_settlement = provider_cost × MARKUP_FACTOR`. The margin (user_settlement - provider_settlement) accumulates in the node's wallet. No Splits contract needed.

5. **NO_CREDIT_BALANCE**: No DB-tracked credit balance. User's ability to pay is determined by their on-chain USDC balance + x402 authorization. Preflight checks wallet balance, not DB balance.

6. **WALLET_PER_NODE**: Each node has its own wallet (for signing outbound x402 to Hyperbolic and receiving inbound x402 from users). This is NOT a Privy-managed wallet — it can be any wallet the node operator controls.

7. **OPEN_SOURCE_MODELS_ONLY**: This architecture supports only models served by Hyperbolic (open-source). No proprietary model fallback in this spec.

## Schema

### Tables Retained

**`charge_receipts`** — Same schema, new columns:

| Column | Type | Change |
| - | - | - |
| `x402_inbound_tx` | text NULL | NEW — x402 settlement tx hash (user→node) |
| `x402_outbound_tx` | text NULL | NEW — x402 settlement tx hash (node→Hyperbolic) |
| `provider_cost_usd` | numeric NULL | NEW — Raw provider cost (pre-markup) for margin tracking |

All existing columns retained. `charged_credits` repurposed as USDC micro-units (same CREDITS_PER_USD = 10M precision).

**`llm_charge_details`** — Retained unchanged. Model, tokens, provider, latency telemetry.

**`virtual_keys`** — Retained. Agent attribution handle. `billing_account_id` FK becomes wallet-address-based.

**`billing_accounts`** — Simplified. No balance column. Becomes wallet→agent registry.

| Column | Type | Change |
| - | - | - |
| `wallet_address` | text | NEW — User's on-chain wallet address (primary identifier) |
| `balance` | — | DELETED — No DB-tracked balance. On-chain USDC is the balance. |

### Tables Deleted

| Table | Why |
| - | - |
| `credit_ledger` | No credit balance to track. x402 settles per-request. |
| `payment_attempts` | No deposit flow. x402 handles payment atomically. |
| `payment_events` | No payment state machine. |

### Tables Not Changed

| Table | Why |
| - | - |
| `llm_charge_details` | Telemetry unchanged |
| Ledger tables (epochs, activity_events, etc.) | Revenue tracking unchanged |

## Design

### 1. E2E Request Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ INBOUND: User/Agent → Cogni Node (x402 upto)                       │
│ ──────────────────────────────────────                              │
│ 1. Agent sends POST /v1/chat/completions                            │
│ 2. Node reads model from request, looks up pricing in litellm config│
│ 3. Node computes maxPayableAmount:                                  │
│    = (input_tokens × input_cost) + (max_tokens × output_cost)       │
│      × USER_PRICE_MARKUP_FACTOR                                     │
│    (pricing.ts: calculateMaxPayable)                                │
│ 4. Node responds 402 with:                                          │
│    { scheme: "upto", maxPayableAmount, resource, chain: "base" }    │
│ 5. Agent signs upto authorization (USDC on Base)                    │
│ 6. Node calls verifyPayment() — confirms authorization covers max   │
│ 7. Proceed to OUTBOUND                                              │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ OUTBOUND: Cogni Node → Hyperbolic (x402 upto)                      │
│ ─────────────────────────────────────────                           │
│ 8. Node sends request to Hyperbolic via LiteLLM                     │
│ 9. Hyperbolic responds 402 with its own maxPayableAmount            │
│10. Node signs upto authorization from its wallet                    │
│11. Hyperbolic verifies, serves inference, streams response          │
│12. Hyperbolic settles actual cost from node wallet                  │
│13. LiteLLM captures response cost (x-litellm-response-cost header) │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ SETTLEMENT: Node settles with user                                  │
│ ──────────────────────────────────                                  │
│14. Node computes user charge:                                       │
│    actual_user_cost = provider_cost × MARKUP_FACTOR                 │
│    (calculateLlmUserCharge — unchanged)                             │
│15. Node calls settlePayment(actual_user_cost) on inbound x402       │
│16. charge_receipt written:                                          │
│    - source_reference = litellm_call_id (idempotent)                │
│    - x402_inbound_tx = user settlement tx hash                      │
│    - x402_outbound_tx = Hyperbolic settlement tx hash               │
│    - provider_cost_usd = raw Hyperbolic cost                        │
│    - response_cost_usd = user cost (with markup)                    │
│17. DAO margin = user_settlement - provider_settlement               │
│    Sits in node wallet until epoch payout                           │
└─────────────────────────────────────────────────────────────────────┘
```

**Why this ordering?** The node must confirm the user can pay (step 6) before incurring provider cost (step 11). But actual settlement happens after completion (steps 12, 15) because the `upto` scheme settles actual cost, not max.

### 2. LiteLLM Configuration Change

LiteLLM routes to Hyperbolic instead of OpenRouter. Hyperbolic exposes an OpenAI-compatible API, so LiteLLM's `openai/` provider prefix works.

**Before (OpenRouter):**
```yaml
- model_name: deepseek-v3.2
  litellm_params:
    model: openrouter/deepseek/deepseek-v3.2
    api_key: "os.environ/OPENROUTER_API_KEY"
```

**After (Hyperbolic):**
```yaml
- model_name: deepseek-v3
  litellm_params:
    model: hyperbolic/deepseek-ai/DeepSeek-V3
    api_key: "os.environ/HYPERBOLIC_API_KEY"
```

LiteLLM has a **native `hyperbolic/` provider prefix** — no `api_base` override needed.

**Model catalog migration:**

| Current (OpenRouter) | Hyperbolic Replacement | Hyperbolic Model ID | $/1M tokens | Notes |
| - | - | - | - | - |
| deepseek-v3.2 | DeepSeek-V3 | `deepseek-ai/DeepSeek-V3` | $0.25 | Direct equivalent |
| deepseek-v3.1 | DeepSeek-V3-0324 | `deepseek-ai/DeepSeek-V3-0324` | $1.25 | Or consolidate to V3 |
| llama-3.3-70b | Llama 3.3 70B | `meta-llama/Llama-3.3-70B-Instruct` | $0.40 | Direct equivalent |
| qwen3-vl-8b | Qwen2.5-VL-72B | `Qwen/Qwen2.5-VL-72B-Instruct` | $0.60 | Upgraded to larger model |
| gpt-4o-mini (free) | Llama 3.1 8B | `meta-llama/Llama-3.1-8B-Instruct` | $0.10 | Free tier replacement |
| kimi-k2.5 | Kimi-K2 | `moonshotai/Kimi-K2` | $2.00 | 1T param MoE |
| kimi-k2-thinking | Kimi-K2 | `moonshotai/Kimi-K2` | $2.00 | Same model |
| — (NEW) | DeepSeek-R1 | `deepseek-ai/DeepSeek-R1` | $2.00 | Reasoning model (no current equivalent) |
| — (NEW) | Qwen3-235B | `Qwen/Qwen3-235B-A22B-2507` | $2.00 | Large MoE |
| — (NEW) | Qwen3-Coder-480B | `Qwen/Qwen3-Coder-480B-A35B` | $2.00 | 262k context coding |
| claude-sonnet-4.5 | **REMOVED** | — | — | No Hyperbolic equivalent |
| claude-opus-4.5 | **REMOVED** | — | — | No Hyperbolic equivalent |
| gpt-5, gpt-5.2 | **REMOVED** | — | — | No Hyperbolic equivalent |
| gemini-2.5-flash, gemini-3-pro, gemini-3-flash | **REMOVED** | — | — | No Hyperbolic equivalent |
| minimax-m2 | **REMOVED** | — | — | No Hyperbolic equivalent |
| grok-4.1-fast | **REMOVED** | — | — | No Hyperbolic equivalent |
| text-embedding-3-small | **SEPARATE PROVIDER** | — | — | Hyperbolic has no embeddings endpoint |

**Pricing note:** Hyperbolic uses **unified per-token pricing** (same rate for input and output tokens). This simplifies max cost estimation: `maxCost = totalTokens × pricePerToken`.

**Embedding gap:** Hyperbolic has no `/v1/embeddings` endpoint. The existing `text-embedding-3-small` must continue routing through an alternative provider (OpenAI direct, Cohere, or a self-hosted model). This is the ONE asymmetric dependency.

**Net impact:** 7 proprietary models + 1 niche model removed. Replaced by competitive open-source equivalents including DeepSeek-R1 (reasoning), Qwen3-Coder-480B (coding), and Kimi-K2 (1T param MoE).

### 3. x402 Middleware Architecture

New middleware intercepts requests before and after LiteLLM processing.

**Inbound x402 middleware** (`src/adapters/server/x402/inbound-middleware.ts`):
- Intercepts `/v1/chat/completions` requests
- If no valid x402 payment header: respond 402 with `upto` payment requirements
- If valid payment header: verify payment, attach billing context to request, proceed to LiteLLM
- After LiteLLM completion: settle actual cost, write charge_receipt

**Outbound x402 client** (`src/adapters/server/x402/outbound-client.ts`):
- Wraps LiteLLM's HTTP calls to Hyperbolic
- Handles 402 responses from Hyperbolic: signs `upto` authorization from node wallet
- Records settlement tx hash for audit trail

**x402 payment verification** (`src/adapters/server/x402/facilitator.ts`):
- Verifies inbound x402 authorizations
- Calls Thirdweb/Coinbase facilitator for settlement
- Can use self-hosted facilitator for sovereignty (P1)

### 4. Pricing Math (Unchanged Core, New Surface)

`calculateLlmUserCharge()` in `pricing.ts` is unchanged. The pipeline:

```
providerCostUsd (from LiteLLM)
  → userCostUsd = providerCostUsd × USER_PRICE_MARKUP_FACTOR
  → chargedCredits = ceil(userCostUsd × CREDITS_PER_USD)
```

New function for x402 max estimate:

```typescript
// pricing.ts — NEW
export function calculateMaxPayable(
  inputTokens: number,
  maxOutputTokens: number,
  inputCostPerToken: number,
  outputCostPerToken: number,
  markupFactor: number
): bigint {
  const maxProviderCost =
    inputTokens * inputCostPerToken + maxOutputTokens * outputCostPerToken;
  const maxUserCost = maxProviderCost * markupFactor;
  return usdToCredits(maxUserCost); // Same ceil rounding
}
```

### 5. DAO Margin Collection

No Splits contract. Margin accumulates naturally:

```
Per request:
  User pays node:      $0.0024 (provider cost × 2.0 markup)
  Node pays Hyperbolic: $0.0012 (actual provider cost)
  Margin in node wallet: $0.0012

Epoch payout:
  Node wallet accumulated margin → DAO treasury (existing epoch settlement)
```

The epoch ledger system (activity_events, epoch_allocations, payout_statements) is unchanged. The `charge_receipts.provider_cost_usd` column enables accurate margin computation per-request.

### 6. Environment Variables

| Variable | Change | Purpose |
| - | - | - |
| `OPENROUTER_API_KEY` | **DELETED** | No longer using OpenRouter |
| `HYPERBOLIC_API_KEY` | **NEW** | Hyperbolic API key (for non-x402 fallback auth) |
| `NODE_WALLET_ADDRESS` | **NEW** | Node's wallet address on Base (for x402 signing) |
| `NODE_WALLET_PRIVATE_KEY` | **NEW** | Node's wallet private key (or keystore path) |
| `X402_FACILITATOR_URL` | **NEW** | x402 facilitator endpoint (default: Coinbase) |
| `X402_CHAIN_ID` | **NEW** | Chain for x402 settlements (default: 8453 = Base) |
| `USER_PRICE_MARKUP_FACTOR` | **KEPT** | Same markup logic |
| `BILLING_INGEST_TOKEN` | **KEPT** | LiteLLM callback auth unchanged |
| `PRIVY_APP_ID`, `PRIVY_APP_SECRET`, `PRIVY_SIGNING_KEY` | **DELETED** | No Privy — node wallet is direct |

### 7. What Gets Deleted

| Component | File(s) | Why |
| - | - | - |
| credit_ledger table | `packages/db-schema/src/billing.ts` | No credit balance |
| payment_attempts table | `packages/db-schema/src/billing.ts` | No deposit flow |
| payment_events table | `packages/db-schema/src/billing.ts` | No payment state machine |
| creditsConfirm.ts | `src/features/payments/services/creditsConfirm.ts` | No credits to confirm |
| creditsSummary.ts | `src/features/payments/services/creditsSummary.ts` | No balance to query |
| paymentService.ts | `src/features/payments/services/paymentService.ts` | Replaced by x402 middleware |
| drizzle-payment-attempt.adapter.ts | `src/adapters/server/payments/` | No payment attempts |
| evm-rpc-onchain-verifier.adapter.ts | `src/adapters/server/payments/` | Replaced by x402 facilitator |
| ponder-onchain-verifier.adapter.ts | `src/adapters/server/payments/` | Replaced by x402 facilitator |
| operator-wallet spec | `docs/spec/operator-wallet.md` | No operator wallet |
| web3-openrouter-payments spec | `docs/spec/web3-openrouter-payments.md` | No OpenRouter |
| Privy integration | All PRIVY_* env vars and adapter code | No Privy |
| Splits contract deployment | Smart contract + scripts | No Splits |
| Coinbase Commerce integration | OpenRouter top-up code | No fiat bridge |
| OpenRouter-specific billing guards | `billing/ingest/route.ts` lines 125-178 | No OpenRouter zero-cost bugs |

### 8. What Survives

| Component | File(s) | Why |
| - | - | - |
| LiteLLM proxy | `platform/infra/services/runtime/` | Cost oracle + routing. Core value. |
| pricing.ts | `src/core/billing/pricing.ts` | `calculateLlmUserCharge()`, markup factor, `CREDITS_PER_USD` |
| charge_receipts | `packages/db-schema/src/billing.ts` | Immutable audit trail. Same idempotency. |
| llm_charge_details | `packages/db-schema/src/billing.ts` | Telemetry unchanged |
| billing ingest route | `src/app/api/internal/billing/ingest/route.ts` | LiteLLM callback endpoint. Settlement logic changes. |
| virtual_keys | `packages/db-schema/src/billing.ts` | Agent attribution |
| billing_accounts (simplified) | `packages/db-schema/src/billing.ts` | Wallet→agent registry (no balance) |
| epoch ledger | `packages/db-schema/src/ledger.ts` | Revenue tracking unchanged |
| model catalog server | `src/shared/ai/model-catalog.server.ts` | Caches model metadata |
| preflight balance check | `src/features/ai/services/completion.ts` | Now checks on-chain USDC via x402, not DB balance |

## Implementation Checklist

### P0: Core x402 Middleware + Hyperbolic Routing

- [ ] x402 inbound middleware: intercept requests, respond 402, verify payment, settle after completion
- [ ] x402 outbound client: handle Hyperbolic 402 responses, sign from node wallet
- [ ] LiteLLM config: replace all OpenRouter model routes with Hyperbolic equivalents
- [ ] Migrate billing_accounts to wallet-address-based identity (no balance column)
- [ ] Add x402 settlement columns to charge_receipts (x402_inbound_tx, x402_outbound_tx, provider_cost_usd)
- [ ] Delete credit_ledger, payment_attempts, payment_events tables + migration
- [ ] Delete creditsConfirm.ts, creditsSummary.ts, paymentService.ts
- [ ] Delete Privy adapter code + env vars
- [ ] Update billing ingest route for x402 settlement instead of credit debit
- [ ] New env vars: NODE_WALLET_ADDRESS, NODE_WALLET_PRIVATE_KEY, X402_FACILITATOR_URL, HYPERBOLIC_API_KEY
- [ ] calculateMaxPayable() in pricing.ts

#### Chores

- [ ] Observability: x402 settlement metrics (inbound/outbound success/failure, latency, margin)
- [ ] Documentation: Update AGENTS.md billing pointers

### P1: Hardening + Multi-Turn Sessions

- [ ] "Sign once, settle many" for multi-turn chat sessions (single authorization, multiple settlements)
- [ ] Self-hosted x402 facilitator (remove Coinbase dependency for verification)
- [ ] Circuit breaker: pause serving if node wallet balance below threshold
- [ ] Grafana dashboard: x402 settlements, margins, wallet balance
- [ ] Embedding model support on Hyperbolic (or alternative provider)

### P2: Sovereignty + Federation

- [ ] L402 (Lightning) as alternative inbound payment rail
- [ ] Pluggable facilitator port (swap Coinbase → self-hosted → community)
- [ ] Cross-node x402 routing (agent pays node A, node A proxies to node B)
- [ ] **Do NOT build this preemptively**

## File Pointers (P0 Scope)

| File | Change |
| - | - |
| `src/adapters/server/x402/inbound-middleware.ts` | NEW — x402 inbound payment verification + settlement |
| `src/adapters/server/x402/outbound-client.ts` | NEW — x402 outbound payment signing for Hyperbolic |
| `src/adapters/server/x402/facilitator.ts` | NEW — x402 facilitator adapter (verify + settle) |
| `src/core/billing/pricing.ts` | ADD `calculateMaxPayable()` |
| `src/app/api/internal/billing/ingest/route.ts` | MODIFY — x402 settlement instead of credit debit |
| `platform/infra/services/runtime/configs/litellm.config.yaml` | REWRITE — Hyperbolic models replacing OpenRouter |
| `packages/db-schema/src/billing.ts` | MODIFY — delete credit_ledger, payment_attempts, payment_events; add x402 columns to charge_receipts |
| `src/features/payments/` | DELETE — creditsConfirm, creditsSummary, paymentService |
| `src/adapters/server/payments/` | DELETE — payment attempt adapter, on-chain verifiers |

## Open Questions

1. **Hyperbolic x402 uses `exact` scheme, not `upto`** — Hyperbolic's x402 endpoint (`hyperbolic-x402.vercel.app`, separate from `api.hyperbolic.xyz`) currently uses the core x402 `exact` scheme with EIP-3009 `transferWithAuthorization`. This means Hyperbolic sets a fixed per-request price, not dynamic per-token. The outbound leg is therefore `exact` (fixed price set by Hyperbolic) while the inbound leg is `upto` (dynamic, settled by us after LiteLLM computes actual cost). **This asymmetry exists at the protocol level, but both legs are x402 USDC on Base** — the difference is only in the scheme field. The x402 source code is at [github.com/HyperbolicLabs/hyperbolic-x402](https://github.com/HyperbolicLabs/hyperbolic-x402).

2. **Hyperbolic x402 endpoint is a Vercel wrapper** — The x402 path is a separate Vercel-deployed proxy, not the core Hyperbolic API. This introduces a dependency on that proxy's availability. Evaluate whether to use x402 outbound (wallet-gated, no API key) vs. standard API key auth (more reliable, established) for the outbound leg. Both options keep x402 on inbound.

3. **No embeddings on Hyperbolic** — Confirmed: no `/v1/embeddings` endpoint. text-embedding-3-small must continue through a separate provider. Options: OpenAI direct (requires fiat), Cohere (has crypto options), or self-hosted (e.g., BGE on Hugging Face). This is the one remaining provider dependency outside Hyperbolic.

4. **Node wallet security** — Private key in env var is a security concern. P1 should evaluate keystore/Vault/CDP wallet alternatives. The `OperatorWalletPort` pattern from the current spec could be retained as `NodeWalletPort`.

5. **Max estimate UX** — The max payable amount can be 10-20x the actual cost (worst case: max_tokens fully used). Mitigation: Hyperbolic's unified per-token pricing makes estimates more predictable than split input/output pricing. Show estimated cost alongside max authorization.

6. **Hyperbolic rate limits** — Basic tier: 60 RPM. Pro: 600 RPM. May be insufficient for production agent workloads. Enterprise tier requires contacting Hyperbolic directly.

## Related

- [Billing Evolution](./billing-evolution.md) — Current billing invariants (many carried forward)
- [Node vs Operator Contract](./node-operator-contract.md) — Sovereignty invariants
- [Node vs Operator x402](./node-operator-x402.md) — Updated node/operator split with x402
- [Gateway Billing Analysis](../research/gateway-billing-analysis.md) — Research backing this spec
- [proj.x402-e2e-migration](../../work/projects/proj.x402-e2e-migration.md) — Project roadmap
- [proj.ai-operator-wallet](../../work/projects/proj.ai-operator-wallet.md) — Superseded by this spec
