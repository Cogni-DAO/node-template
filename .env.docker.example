# Docker Stack Environment
# Used ONLY by docker-compose for container environments
# NEVER loaded by host Vitest or other host tools
# Copy this to .env.docker (never commit .env.docker)

# Either `test`, or `production'. AI calls mocked in `test` mode
NODE_ENV=test
APP_ENV=test

# For your own sanity, don't change these (or update .env.stack.local to match)
POSTGRES_DB=cogni_template_stack_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Container-to-container DATABASE_URL (postgres = container name)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Container app URL for tests
DOMAIN=localhost
APP_BASE_URL=https://localhost
NEXTAUTH_URL=https://localhost

# LLM config for container
LITELLM_BASE_URL=http://litellm:4000
LITELLM_MASTER_KEY=test-key
OPENROUTER_API_KEY=test-key
DEFAULT_MODEL=openrouter/auto

# Other container settings
PORT=3000
SESSION_SECRET=docker-stack-secret-at-least-32-characters-long
PINO_LOG_LEVEL=debug