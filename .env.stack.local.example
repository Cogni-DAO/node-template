# Stack Test Environment Overrides
# Used for stack tests - overrides .env.local with test-specific values
# Copy this to .env.stack.local (never commit .env.stack.local)

# For running against `pnpm dev:stack` (host dev server + host DB)
# POSTGRES_DB=cogni_template_stack_test
# APP_ENV=test
# DB_PORT=5432
# TEST_BASE_URL=http://localhost:3000/
# DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${DB_PORT}/${POSTGRES_DB}"

# For running against `pnpm docker:dev:stack` (containerized app + exposed DB port)
# Note: App-specific env vars are loaded during docker:dev:stack launch. Only test connection vars should be set here.
DB_PORT=55432
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${DB_PORT}/${POSTGRES_DB}"
TEST_BASE_URL=https://localhost/

# For running against `pnpm docker:stack` (full containerized production-like stack)
# DB_PORT=55432
# DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${DB_PORT}/${POSTGRES_DB}"
# TEST_BASE_URL=https://localhost/
